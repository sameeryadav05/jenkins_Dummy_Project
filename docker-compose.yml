

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend
    ports:
      - 5000:5000
    environment:
      - PORT=5000
      - MONGODB_URL=mongodb+srv://sameermanoj2005_db_user:6YwXae7T2E5oUSZW@cluster0.2o6uiiq.mongodb.net/jenkins
    networks:
      - project

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - 5173:80
    networks:
      - project
  

networks:
  project:
  
    